export enum tagTypes {

   admin = 'admin',

   user = 'user',
   projects = 'projects',
   blogs = 'blogs',

}

export const tagTypesList = [

   tagTypes.admin,

   tagTypes.user,
   tagTypes.projects,
   tagTypes.blogs,

];
